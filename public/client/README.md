# Forms101: Interactive HTML Forms & Validation Guide

This is a static website designed to teach high school students about HTML Forms and Validation. It uses pure HTML, CSS (Bulma), and Vanilla JavaScript.

## 🚀 How to Deploy to GitHub Pages

1.  **Download/Clone:** Get the contents of the `client` folder.
2.  **Upload:** Upload these files to your GitHub repository (either the root or a folder).
3.  **Enable Pages:** Go to your GitHub Repository Settings -> Pages.
4.  **Select Source:** Choose "Deploy from branch" and select `main` (or `master`) and the folder `/` (root) if you uploaded there.
5.  **Save:** Click Save. GitHub will give you a link (e.g., `https://username.github.io/repo-name/`).

## 📁 File Structure

*   `index.html` - The landing page.
*   `forms.html` - The Forms 101 tutorial.
*   `validation.html` - The Validation tutorial.
*   `assets/`
    *   `css/style.css` - Custom styling (Design Engineer polish).
    *   `js/main.js` - The logic for the live code playgrounds.
    *   `images/` - Generated illustrations.

## 🛠 Tech Stack

*   **HTML5**
*   **CSS:** [Bulma CSS Framework](https://bulma.io/) (via CDN) + Custom Styles.
*   **JS:** Vanilla JavaScript (No frameworks, no build step).
*   **Icons:** FontAwesome (via CDN).
*   **Fonts:** Google Fonts (DM Sans, Outfit, Fira Code).

## 📝 For Teachers

The code examples follow the `function name() {}` pattern as requested. The playgrounds run entirely in the browser, so students can experiment safely without breaking anything.
